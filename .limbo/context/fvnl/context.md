## Description
Store previous content when units are updated so changes can be reviewed historically. Could use a unit_history table with (unit_id, old_content, old_type, changed_at) populated by the edit command or a trigger.
